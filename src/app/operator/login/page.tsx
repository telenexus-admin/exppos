import { OperatorLoginForm } from "./operator-login-form";
export default async function OperatorLogin({searchParams}:{searchParams:Promise<{next?:string}>}){const{next}=await searchParams;return <main className="auth operator"><section className="auth-copy"><a className="brand" href="/">Speedyhive<span>Operator Console</span></a><h1>Platform control, without tenant exposure.</h1><p>Onboard and manage subscriptions through a separately secured operator context.</p></section><OperatorLoginForm nextPath={next}/></main>}
