import{OperatorShell}from"@/components/operator-shell";import{DeleteTenantButton}from"@/components/delete-tenant-button";import{db}from"@/lib/db";
export default async function Page({searchParams}:{searchParams:Promise<{q?:string;status?:string;plan?:string;deleted?:string}>}){const query=await searchParams;const tenants=await db.tenant.findMany({where:{status:{not:"CANCELLED"},...(query.q?{OR:[{name:{contains:query.q,mode:"insensitive"}},{code:{contains:query.q,mode:"insensitive"}},{slug:{contains:query.q,mode:"insensitive"}}]}:{}),...(query.status&&query.status!=="all"?{status:query.status.toUpperCase().replace(" ","_") as never}:{}),...(query.plan&&query.plan!=="all"?{subscription:{plan:{name:query.plan}}}:{})},include:{subscription:{include:{plan:true}},_count:{select:{branches:true,users:true}}},orderBy:{createdAt:"desc"}});return <OperatorShell title="POS clients" current="tenants">{query.deleted&&<div className="operator-notice">✓ POS client removed from the active list. Records remain available for audit and recovery.</div>}<form className="operator-toolbar" method="get"><div className="operator-search"><input name="q" defaultValue={query.q} placeholder="Search business, code or tenant slug…"/><button type="submit">Search</button></div><select name="status" defaultValue={query.status??"all"}><option value="all">All statuses</option><option value="ACTIVE">Active</option><option value="TRIAL">Trial</option><option value="GRACE_PERIOD">Grace period</option><option value="SUSPENDED">Suspended</option></select><select name="plan" defaultValue={query.plan??"all"}><option value="all">All plans</option><option>Starter</option><option>Growth</option><option>Business</option></select><button type="submit">Apply filters</button></form><article className="operator-card tenant-table"><div className="operator-table-row operator-table-head"><span>Business</span><span>Plan</span><span>Usage</span><span>Created</span><span>Subscription</span><span>Actions</span></div>{tenants.map(t=><div className="operator-table-row" key={t.id}><a className="business-cell row-link" href={`/operator/tenants/${t.slug}`}><i>{t.name.split(" ").map(x=>x[0]).slice(0,2)}</i><span><strong>{t.name}</strong><small>{t.code} · {t.slug}</small></span></a><span>{t.subscription?.plan.name??"No plan"}</span><span>{t._count.branches} branches<br/><small>{t._count.users} users</small></span><span>{t.createdAt.toLocaleDateString("en-KE")}</span><em className={`tenant-status ${t.status.toLowerCase().replace("_","-")}`}>{t.status.replaceAll("_"," ")}</em><DeleteTenantButton id={t.id} name={t.name}/></div>)}{tenants.length===0&&<div className="operator-empty">No POS clients match these filters. <a href="/operator/tenants">Clear filters</a></div>}</article></OperatorShell>}
