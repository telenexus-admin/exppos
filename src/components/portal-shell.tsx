import type { ReactNode } from "react";
const sections = [["Dashboard","dashboard"],["POS Checkout","pos"],["Branches","branches"],["Staff","staff"],["Customers","customers"],["Products","products"],["Inventory","inventory"],["Purchases","purchases"],["Sales","sales"],["Invoices","invoices"],["Accounting","accounting"],["Reports","reports"],["Tasks","tasks"],["Audit Logs","audit-logs"],["Settings","settings"]] as const;
export function PortalShell({ title, role, current = "dashboard", basePath = "/app", children }: { title: string; role: string; current?: string; basePath?: string; children: ReactNode }) {
  return <div className="portal"><aside><a className="brand" href={`${basePath}/dashboard`}>Speedyhive<span>Cloud POS</span></a><nav>{sections.map(([item, slug]) => <a className={current === slug ? "active" : ""} href={`${basePath}/${slug}`} key={slug}>{item}</a>)}</nav><div className="profile"><span className="avatar">SH</span><div><strong>{role}</strong><small>Head Office</small></div></div></aside><section className="workspace"><header><div><small>Business workspace</small><h2>{title}</h2></div><div className="header-actions"><a className="notification-link" href={`${basePath}/notifications`}>Notifications</a><a className="primary action-link" href="/app/pos">New sale</a></div></header>{children}</section></div>;
}
